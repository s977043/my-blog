---
title: "プラグインが計画のサイクルを支える"
---

ここまで、計画に何を書くかを見てきました。最終章では、PlanGate のプラグインがこのサイクルをどう支えるかを見ます。

第 3 章から第 9 章の内容は、ツールがなくても実践できます。それでも毎回、同じ欄を思い出し、同じ順番で確かめるのは大変です。プラグインは、その手順を毎回同じ形で回せるようにするためのものです。

## 導入する

PlanGate は、Claude Code と Codex のプラグインとして配布されています。導入の手順はツールによって違います。

**Claude Code**（セッションの中で実行します）

```text
/plugin marketplace add s977043/PlanGate
/plugin install plangate
```

**Codex**（マーケットプレイスの登録と、プラグインの追加が別のコマンドです）

```bash
codex plugin marketplace add s977043/PlanGate
codex plugin add plangate@plangate
```

Codex では、1 行目の登録だけではプラグインは読み込まれません。2 行目まで実行してください。

手順は更新されることがあるので、実際に導入するときは[公式 README](https://github.com/s977043/PlanGate/blob/main/README.md) を確認してください。

## プラグインが回すサイクル

プラグインを入れると、第 1 章で見た「入力 → 計画 → レビュー → 実行」を、決まった手順で回せるようになります。

`TASK-0001` は、タスクごとに自分で付ける番号です。この番号でタスクごとの作業用フォルダ（`docs/working/TASK-0001/`）ができ、入力も計画も検証の結果も、そこにまとまります。人が書く入力（PBI INPUT、第 3 章）は、このフォルダの `pbi-input.md` に書きます。第 9 章の骨組みでいえば、（人）の付いた上の欄です。

Claude Code なら、次のようなコマンドで進めます。

```text
/working-context TASK-0001        作業用フォルダを用意する（既にあれば状態を読み込む）
（pbi-input.md に、Why・やらないこと・受入基準などを書く）
/ai-dev-workflow TASK-0001 plan   計画・作業分解・テストケースを作り、レビューの指摘反映まで進める
（人が計画を読んで承認する）
/ai-dev-workflow TASK-0001 exec   承認された計画どおりに実装し、検証する
```

入力を書く前に、アイデアを対話で整理したいときは `/ai-dev-workflow TASK-0001 brainstorm` から始めることもできます。対話の結果は `pbi-input.md` にまとまります。

Codex では、同梱されているスキルを、依頼文の中で `$` を付けて名前で指定して呼び出します。計画づくりなら、たとえば次のように頼みます。

```text
$ai-dev-plan TASK-0001 の pbi-input.md から計画を作って
```

各段階で、プラグインは本書で見てきたことを AI に求めます。

| 段階 | プラグインが AI に求めること | 本書の章 |
|---|---|---|
| 計画の前 | 確認質問で曖昧さをつぶす（最大 3 問） | 第 3 章 |
| 計画づくり | 設計案の比較、Mode の判定、変更の種類に合わせたテスト方針 | 第 4 章 |
| 計画づくり | 前提の実測、件数の確認 | 第 5 章 |
| 計画づくり | やらないこと、止まる条件、人が承認する範囲 | 第 6 章 |
| 計画づくり | 計画と同時にテストケースを作る | 第 7 章 |
| 承認の前 | セルフレビュー。Mode によっては別の AI によるレビュー | 第 8 章 |
| 承認 | 人が承認するまで実装に進まない | 第 8 章 |
| 実行 | 前提が崩れたら止まって計画に戻る。決めた方法で検証する | 第 6・7 章 |

作業の記録（入力、計画、承認、検証の結果、引き継ぎのメモ）は、作業用フォルダに残ります。後から「なぜこの実装になったのか」を追えるのは、この記録があるからです。

## Hook は、承認した計画を守らせる手段の一つ

PlanGate には、承認前の実装などを検知して警告したり止めたりする Hook もあります。止められるのは第 1 章のとおり機械で判定できる範囲だけで、使うときの注意は付録にまとめました。

## 最終判断は人に残る

プラグインを使っても、要件が正しいかの判断、計画の承認と成果物の受け入れ、元に戻せない操作は人に残ります（第 1 章）。PlanGate は、AI にどこまで任せ、どこで人が判断するかを、毎回同じ形で決めるための仕組みです。任せる範囲を広げたいときも、まず計画の書き方を見直すところから始めるのが近道です。

## PlanGate を使わない場合でも

本書で扱った内容は、PlanGate に閉じたものではありません。第 9 章の骨組みを、Issue や PR のテンプレート、AI への指示書に貼るところから始められます。チームで取り入れる順番は、第 9 章の「チームで始める」にまとめました。PlanGate は、それを毎回同じ形で回すための一つの実装です。

PlanGate の最新情報は[公式リポジトリ](https://github.com/s977043/PlanGate)で確認できます。Issue や Discussion でのフィードバックも歓迎です。
