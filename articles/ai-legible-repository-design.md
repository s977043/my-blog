---
title: "AIが迷わないリポジトリ設計：長いプロンプトより先に整える4つの置き場所"
emoji: "🗺️"
type: "tech"
topics: ["ai", "claudecode", "codex", "documentation", "開発環境"]
published: false
---

:::message
この記事は、[Growth Lab](https://the3396.com/articles) の「AIが迷わないリポジトリ設計」メモをもとに、Zenn向けに最小構成へ落とし込んだ下書きです。
:::

Claude Code や Codex を使い始めると、最初にやりたくなるのは「AI向けの長い指示ファイル」を作ることです。

もちろん、入口ファイルは大事です。
ただ、そこに全部を詰め込むと、しばらくして次の問題が出ます。

- 重要なルールが長文の中に埋もれる
- 領域ごとの注意点が混ざる
- 守らせたい制約が自然言語のお願いで止まる
- どの情報が正本か分からず、AIが推測で進む

AIに読ませる文章を増やす前に、**リポジトリ側の置き場所を分ける**ほうが効くことがあります。

## TL;DR

- `AGENTS.md` や `CLAUDE.md` は入口に徹する
- 詳細ルールは局所ファイルや docs に逃がす
- 必ず守らせたい制約は hooks や CI に寄せる
- 変わる情報は手書きのMarkdownに固定しない

## 1. 入口ファイルは短くする

`AGENTS.md` や `CLAUDE.md` に置きたいのは、全セッションで必要な情報です。

- このリポジトリは何をするものか
- 主要ディレクトリの責務は何か
- どのコマンドで検証するか
- どのファイルが正本か
- 触ってはいけない領域はどこか

逆に、たまにしか使わない手順や、特定領域だけの細かい注意点まで入れると、入口が重くなります。

入口ファイルは、百科事典ではなく地図です。

## 2. 危険領域には局所ルールを置く

認証、課金、DBマイグレーション、公開フローのような領域は、全体ルールだけでは足りないことがあります。

たとえば、こう分けます。

```text
repo-root/
├── AGENTS.md
├── docs/
│   ├── architecture.md
│   └── runbook.md
├── src/
│   ├── auth/
│   │   └── AGENTS.md
│   └── billing/
│       └── AGENTS.md
└── spec/
    ├── requirements.md
    ├── design.md
    └── tasks.md
```

`src/auth/AGENTS.md` には、認証まわりの禁止事項や確認観点だけを書きます。
全体に効く話と、局所的に効く話を混ぜないのがポイントです。

## 3. 強制したいことは仕組みに寄せる

自然言語のルールは、あくまで文脈です。
AIは読んで従おうとしますが、必ず守る保証にはなりません。

だから、必ず守らせたいことは hooks、CI、テスト、ブランチ保護などに寄せます。

- format を必ず走らせる
- 特定パス変更時にテストを走らせる
- secret を検出したら止める
- main への直接 push を防ぐ
- migration 変更時にレビューを必須にする

AIに「気をつけて」と書くより、通れないゲートを作るほうが運用として強いです。

## 4. 変わる情報をMarkdownに抱え込まない

PRの状態、Issueの担当者、外部サービスの現在値、APIのレスポンスなどは変わります。

こうした情報を手書きのMarkdownに固定すると、すぐ古くなります。

リポジトリに置くべきなのは、長く効く構造化された知識です。
変わる情報は、GitHub、Notion、MCP、APIなど、その時点の正本から取りに行くほうが安全です。

```text
長く効くもの: docs / spec / runbook / ADR
変わるもの: PR状態 / Issue状態 / 外部APIの現在値 / 本番データ
```

この境界があると、AIが古いメモを信じて進む事故を減らせます。

## 最小チェックリスト

AI向けにリポジトリを整えるなら、まずはこの7点を見ます。

- 入口ファイルが長くなりすぎていないか
- 目的、地図、検証コマンド、正本が分かるか
- 危険領域の局所ルールがあるか
- 必ず守る制約がCIやhooksに寄っているか
- 仕様、設計、タスクの置き場所が分かれているか
- 変わる情報をMarkdownに固定していないか
- AIが迷ったとき、どこを読めばよいか分かるか

## まとめ

AIが迷わないリポジトリを作るうえで、長いプロンプトは最初の答えではありません。

まずは置き場所を分けます。

- 入口ファイル
- 局所ルール
- 強制する仕組み
- 正本と外部参照

この4つが分かれているだけで、AIは推測で進みにくくなります。

AIに何を言うかより先に、AIがどこを見ればよいかを設計する。
ここから始めると、AIコーディングの安定性はかなり変わります。

## 参考

- [Growth Lab](https://the3396.com/articles)
- [DESIGN.md 導入ガイド: AI実装のための入口・契約・検証をどう整えるか](https://zenn.dev/minewo/articles/design-md-guide-and-adoption-log)
- [アジャイルでAI駆動開発をどう回すか: PlanGateの考え方とテンプレート](https://zenn.dev/minewo/articles/plangate-ai-coding-workflow)
- [AI駆動開発の2層ガード設計：PlanGateとRiver Reviewerで実装前後を守る](https://zenn.dev/minewo/articles/ai-dev-guardrail-plangate-river-reviewer)
