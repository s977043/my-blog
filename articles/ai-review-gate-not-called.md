---
title: "品質ゲートは効かなかったのではなく、呼ばれなかった"
emoji: "📉"
type: "tech"
topics: ["ai駆動開発", "claudecode", "aiエージェント", "品質管理", "開発生産性"]
published: false
---

:::message
**この記事で得られること**

- 品質ゲートは「効くかどうか」の前に「呼ばれるかどうか」を測る必要がある、という視点
- ゲートが呼び出し・迂回・完走・集計・判断の5段階で別々に漏れること
- 強制しない品質活動を続けるための「採否は自由、記録は必須」という形

**想定読者**: AIコーディングエージェントを日常運用し、レビューや検査の仕組みを自分で組んでいる人
**この記事の規模**: 1か月・記事8本・ゲート通過2本・findings 9件。この規模で一般化するのは「ゲートを測る」という発想の1点に限ります
:::

## はじめに：設計は動いた。ただ、ほとんど呼ばれていなかった

AIに技術記事を書かせると、独特の定型感が残ります。同じ接続詞が段落頭に並ぶ。文末が揃う。根拠のないまま「重要です」で締める。

この「AIっぽさ」を消すツールは既にいくつもありますが、そのほとんどは**本文を書き換えます**。技術記事でそれをやると、主張・数値・コード・引用が一緒に書き換わる可能性があります。文体を直したいだけなのに、事実が動く。

そこで、**検出だけして書き換えないレビュー**を作りました。2026年7月17日のことです。

設計自体は動きました。根拠の薄い数値を1件、私に差し戻してくれました。

そして1か月後、数えてみました。

**この間に8本の記事を書き直し、そのうちレビューを通したのは2本でした。**

効果があったかどうかを議論する以前の話です。私は、このゲートが何回呼ばれたのかを一度も数えていませんでした。

この記事は、その記録です。

---

## 検出と修正を分離する — review-only を指示ではなく権限で縛る

まず、何を作ったのかを簡単に共有します。この記事の主題は設計ではなく運用なので、要点だけにします。

出発点の仮説は「**文体の改善は、主張の改変と地続きになる**」でした。「この段落は冗長です」という指摘に従って書き直すと、削られた部分に主張が含まれていることがあります。だから、指摘と修正を構造的に分離することにしました。

具体的には3つです。

**1. 保護リスト**

Front Matter、コードブロック、URL、引用、数値・日付・バージョン、製品名やAPI名、そして**記事の主張**と**筆者の実体験**。これらは指摘の対象にしません。文体上の問題が保護領域を含む文にある場合も、保護要素そのものは変えず、周辺の文だけを対象にします。

**2. リスク昇格**

保護領域に触れる可能性がある指摘は、内容にかかわらず `high` へ引き上げます。「この数値の根拠が薄い」という指摘は、たとえ表現の問題として出てきても、数値に触れる以上は `high` です。`high` は自動修正の対象外で、著者の判断に戻します。

**3. 権限による強制**

そして、これが一番効いていると考えている部分です。

```yaml
allowed-tools:
  - Read
  - Grep
  - Glob
```

このレビューを実行するスキルには、`Edit` も `Write` も渡していません。「本文を書き換えないでください」という指示ではなく、**書き換える手段を持たせない**という形にしました。

> 指示は破られますが、権限は破れません。

この方針が崩れていないかは、静的チェックで機械的に検証しています。`Edit` / `Write` / `Bash` が `allowed-tools` に混入したら失敗します。

```bash
npm run test:article-humanizer
```

ただし、正直に書いておくべき非対称があります。`allowed-tools` が効くのはこのスキルを実行するときだけで、レビュー全体を回すワークフローの他の工程は `Edit` を持っています。そちらは自然言語の禁止文で守っている。**同じ仕組みの中に「権限で守っている領域」と「言葉で守っている領域」が併存しています。**

この非対称は、後で効いてきます。

---

## 止めるか、止めないかで考えていた

もう一つ、当時決めたことがあります。

**このレビューで `high` の指摘が出ても、記事の公開はブロックしない。**

理由は明確でした。ブロックすると、AIの文体判定が人間の主張を却下する構造になります。「この段落は根拠が薄い」という判定で公開が止まるなら、それは文体レビューではなく検閲です。主張を守るために作った仕組みが、主張を却下する側に回るのは本末転倒です。

この判断自体は、今でも正しかったと考えています。

ただし、**私は「止めるか、止めないか」の二択で考えていました。そこが誤りでした。**

第三の形があります。

> **採否は自由。ただし、記録は必須。**

指摘を受け入れるかどうかは著者が決める。しかし「確認した」という事実だけは残す。ブロックはしないが、素通りもできない。

このときの私は、この形に気づいていませんでした。

---

## 1か月後に数えたら、8本中2本だった

2026年8月、ゲートの導入から1か月が経ったので、状況を確認しました。

まず、この期間に触った記事の本数です。

```bash
git log --since=2026-07-17 --name-only --pretty=format: -- articles/ | sort -u
```

8本ありました。新規執筆も、既存記事の改稿も含みます。

次に、レビューを通した記事です。このレビューは成果物をファイルとして残すので、そのディレクトリを見れば分かります。

```bash
git log --since=2026-07-17 --name-only --pretty=format: -- reviews/zenn/ | sort -u
```

**2本でした。**

採用率25%。8本のうち6本は、ゲートを通さないまま公開・更新されています。

弁解として「忙しくて回せなかった」と言いたくなりますが、データがそれを否定します。**6本のうち1本は、3周のレビューを回しながら、この工程だけを飛ばしていました。** レビュー自体は丁寧にやっている。ただ、このゲートだけが呼ばれていない。

なぜそうなるかというと、ゲートがワークフローの外側にあり、**迂回できる**からです。他のレビューを回すことと、このゲートを通すことは、手順として繋がっていませんでした。

ここで言えることと、言えないことを分けておきます。

- **言えること**: このゲートは呼ばれていなかった（母集団8本に対する実測）
- **言えないこと**: このゲートに効果があるかどうか（通過2本では判定できません）

効果を測る以前に、呼ばれた回数を測っていませんでした。

---

## ゲートは5段階で別々に漏れる

数え始めると、漏れは「呼ばれない」だけではありませんでした。しかも、それぞれ原因が違います。

| # | 段階 | 何が起きたか |
|---|---|---|
| 1 | 呼ばれる | 8本中2本しか通していない |
| 2 | 迂回されない | 3周のレビューを回しながら、この工程だけスキップ |
| 3 | 完走する | 記録工程がモデルの利用上限で落ち、成果物を手で作り直した |
| 4 | 集計できる | 検出パターンの識別子が集計キーとして使えなかった |
| 5 | 判断に使う | 指摘が不採用のまま公開された |

段階1と2は前章のとおりです。残りを見ていきます。

### 段階3：記録工程が落ちる

2回目の実行で、レビュー結果をファイルに書き出す工程が**モデルの利用上限に達して失敗**しました。結果自体は返ってきていたので、成果物は手で作り直しました。

このとき「実行は失敗したが結果は手元にある」と処理してしまい、事故として扱いませんでした。しかしこれは、**その回の生データが失われた**ということです。後から検証しようとしても、手で書き直したものしか残っていない。

### 段階4：識別子が集計キーにならない

検出パターンには `F01`〜`F05`（書式）、`S01`〜`S14`（語彙・文体）、`T01`〜`T15`（論証構造）という識別子を定義してあります。合計34個です。

実運用で記録された9件の識別子を並べると、こうなっていました。

- `T04` / `F05` / `S08` — 定義どおりの識別子
- `S12-recurring-importance-closer` / `S10-list-intro-template` / `S08-corrective-frame-density` — 識別子＋自由文のスラッグ
- `F-table-header-empty-cell` — 連番すらない

混在しています。当初、私はこれを「スキーマで列挙型を指定しなかったから、モデルが値を創作した」と解釈しました。実際、同じスキーマの中で `layer` と `risk` は列挙型で縛ってあり、そちらは一度も壊れていません。

**しかし、それは正しい説明ではありませんでした。**

スキル定義書の出力例を見ると、こう書いてあります。

```json
"pattern": "S01-repeated-connectors"
```

**識別子＋スラッグの複合形を、仕様書自身が例として教えていました。** 一方、正本のパターン表は裸の識別子です。仕様が自己矛盾していて、モデルは例のほうに従っていた。制約の欠如だけが原因ではありません。

そして、書式の混在よりも厄介な問題があります。**識別子は合っているのに、意味が違う**ケースです。

`S12` の定義は「意義づけの付け足し（事実の後に『示唆している』『浮き彫りにする』を反復する）」ですが、実際の指摘内容は「各章の締めが『重要です』で単調」でした。別のパターンです。識別子だけ見ていると気づきません。

さらに根が深いのは、**構造化された結果がどこにも永続化されていない**ことです。記録工程は、生のJSONをモデルに渡して「読みやすいMarkdownにしてください」と依頼する形になっています。決定論的にJSONを保存する経路がありません。仮に識別子へ制約を張って正しい値が返っても、**この転記で潰れれば結果は同じ**です。

:::message
**一般化の限度について**

「制約を張らない列は必ず創作される」とは書けません。同じスキーマの `id` は列挙型ではなく正規表現 `^H-[0-9]{3}$` で縛られており、9件すべてで守られています。正しくは「**制約のない列は drift し得る**」であり、今回は仕様例の不整合という別要因も重なっています。統制された対照実験ではなく、自然観察です。
:::

### 段階5：指摘が不採用のまま公開される

3回目の実行では、`low` の指摘が2件出ました。「章の締めの語彙が単調」「箇条書きの導入が同じ鋳型で反復」というものです。

その記事は既に公開されています。確認してみます。

```bash
grep -c "重要です\|重要になります" articles/ai-merge-ready-state-machine.md
# → 4
grep -c "たとえば、次の" articles/ai-merge-ready-state-machine.md
# → 3
```

指摘された表現が、そのまま残っています。

これ自体は設計どおりです。`low` は任意の推敲であり、ブロックしません。問題は、**採用しなかったという判断がどこにも記録されていない**ことです。検討した上で見送ったのか、そもそも読んでいないのか、後から区別できません。

---

## なぜ1か月気づかなかったか

5つの漏れは、原因がそれぞれ違います。呼び出しの導線がないこと、工程が分離していること、モデルの利用上限、仕様書の自己矛盾、記録の不在。ひとつの原因に還元できる話ではありません。

ただ、共通していることが1つだけあります。

> **どの段階も、回数を数える仕組みを持っていませんでした。**

- 呼ばれた回数を数えていない → 採用率25%に気づかない
- 完走した回数を数えていない → 記録工程の失敗が事故にならない
- 記録の形式を検査していない → 識別子の drift が見えない
- 採否を数えていない → 指摘が無視され続けても誰も困らない

### 器は用意してあった

ここが自分でも意外だった点です。**記録の場所は最初から用意してありました。**

スキル定義書には「## 誤検知・過剰指摘」という記録欄が定義してあります。誤検知が出たらここに書いて、パターンの調整に使う想定でした。

その形式で書かれた成果物は、**1本も存在しません**。

理由を追うと、記録工程のプロンプトが検出結果しか要求しておらず、**誤検知欄に何かを書けとは一言も指示していませんでした**。器は用意したが、器へ流し込む配線がなかった。

置き場所があることと、情報が流れることは別です。私は前者をもって後者を確保したつもりになっていました。

### 空の器を機能させる最小条件

同じ構造は、ツールの外側でもよく見ます。

| 空の器 | 埋まらない理由 | 機能させる最小条件 |
|---|---|---|
| ふりかえりのアクション欄 | 実行しなくても次が回る | 次のふりかえりを、前回アクションの結果から始める |
| 品質レビューの記録欄（本記事） | 無視しても誰も困らない | 採否の記録だけを必須にする（中身は自由） |
| チェックリストの項目 | 判定根拠が人手で検証不能 | 判定を自動化するか、見る人を1人指名する |

共通するのは、器を機能させるのは置き場所ではないということです。**①誰が ②いつ ③何を見て ④どう判断するか**、この4点が決まって初めて器は埋まります。

### 経験主義の三本柱として見ると

この構造は、スクラムの経験主義（透明性・検査・適応）と同じ形をしています。透明性がなければ検査が成立せず、検査がなければ適応が無意味になる、という依存関係です。

ただし、**ドミノのように順番に倒れたのではありません**。検査と適応は一度機能してから壊れたのではなく、土台がないので最初から成立し得なかった。倒れた順番ではなく、積み上がらなかった順番です。

ひとつ限定を付けます。この運用は、設計者も実行者も記録者も私ひとりです。透明性が本来要求する「作業する人にも受け取る人にも見えている」という二者性は成立していません。ここで言っているのはその退化形——**将来の自分と、検査の材料に対して見えていたか**——です。チームであれば、同じ穴はレビューやふりかえりで先に露見した可能性が高いと思います。

なお、「段階1が終わったら段階2へ進む」という移行条件は、当初「誤検知したパターンを調整済みであること」と決めていました。しかし段階4のとおり、**誤検知率を算出する手段が初日から存在しませんでした**。

測る手段のない完了条件は、条件ではなく願望です。

---

## 採否は自由、記録は必須 — 次に変える6点

直す順番には主張があります。**透明性から直す。ただし、透明性だけ直しても同じことが起きます。**

見る場と判断基準がなければ、可視化された数値は新しい空の器になるだけです。だから検査と適応まで一続きで置きます。

**透明性**

1. **検出結果の生JSONを、ワークフローから直接ファイルへ書く**（モデルを経由しない）。段階3と段階4の共通原因なので、これが最優先です
2. **識別子に制約を張る。同時に、仕様書の出力例を裸の識別子へ直す**。例を直さないと、仕様に忠実な出力がスキーマで弾かれます。この依存関係を見落とすと、直したつもりで壊れます
3. **出力側を静的チェックで検証する**。現在のチェックはスキル定義（入力側）しか見ていません。ただし1が前提です

**検査**

4. **見る場を固定する**。記事を1本公開するたびに、採用率・完走率・パターン別の内訳・採否を確認する。頻度は「記事公開ごと」——これが自分にとってのケイデンスです

**適応**

5. **閾値を、測る前に決める**。「誤検知率がX%を超えたら、そのパターンを落とす」。測ってから決めると、都合のいい線を引きます

**設計**

6. **採否の記録を完了条件に入れる（採否の中身は問わない）**。前章の第三の道です。受け入れは強制せず、確認した事実だけを残す。これは止める仕組みではないので、「公開をブロックしない」という判断を1ミリも曲げません

:::message alert
**チームに持ち込むときの注意**

これらの数字は、プロセスと成果物を検査するためのものであって、**人を検査するためのものではありません**。評価や査定に使った瞬間、数字は作られるようになります。透明性のために入れた仕組みが、透明性を壊します。
:::

---

## おわりに：終わっているのは検査までです

検出と修正を分離する設計は動きました。保護リストとリスク昇格は、根拠の薄い数値を1件、私に差し戻しました。ただし正確に言えば、**止めたのは仕組みで、直したのは人**です。そのまま無視して公開することもできました。

そして1か月後に数えたら、8本中2本しか通していませんでした。ゲートは、呼び出し・迂回・完走・集計・判断の5つの段階で、それぞれ別の理由で漏れていました。共通していたのは、**どの段階も回数を数えていなかった**ことだけです。

強制しない品質活動が消えるのは、動機の問題ではありません。呼ばれた回数を数えていないからです。

この記事の時点で終わっているのは、**検査までです**。適応——上に挙げた6点の実装と、閾値の設定——はこれからです。その結果は、数字とともに続報として出します。

うまくいかなかった過程を書いたこと自体を成果だと言い切ってしまうと、それこそ「器を用意して満足する」の再演になります。透明性は、次の観察がここに戻ってきて初めて成立します。

---

## 関連記事

- [AIエージェントを勝手に成長させない — 記録・棚卸し・機械化・剪定の自己改善ループ](https://zenn.dev/minewo/articles/ai-agent-self-improvement-loop-design)
- [AIにマージさせない。PRをMERGE_READYまで運ぶ状態機械の設計](https://zenn.dev/minewo/articles/ai-merge-ready-state-machine)
