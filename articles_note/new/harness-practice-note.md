# AIに書かせる仕組みではなく、AIが安全に間違えられる仕組みを作っていた

> 区分: 個人

五反田のスタートアップでプロダクト開発に携わっている、みね（[@mine\_take](https://x.com/mine_take)）です。普段は、AIを使った開発プロセスの改善や、チームでの意思決定の進め方について考えています。

※本記事は個人の活動として執筆したものであり、所属組織の公式見解ではありません。

## 「できました」が信用できなくなった話から始まります

AI駆動開発を始めたころ、私がやっていたことは単純でした。エージェントに実装を頼み、出てきた差分を眺め、良さそうならマージする。それだけです。

うまくいっているように見えました。実際、速くはなりました。

つまずいたのは、速さではなく報告の中身のほうでした。

ある日、リネームと内容編集をまとめて1コミットにする作業をエージェントに任せました。完了報告は問題なしでした。ところが実際にマージされたPRは「7 files changed, 0 insertions」で、リネームだけが入り、本文の編集がまるごと落ちていました。さらにその後片付けで、復旧できたはずの変更を `git stash drop` で消しました。リカバリに丸ごと1本のPRを使いました。

別の日には、Qiitaの重複記事を「人間が手で消すしかない不可逆のブロッカー」として何ラウンドも申し送りました。最後にURLを叩いたら404でした。記事はそもそも存在していませんでした。ローカルにファイルが残っていただけで、リモートの状態を一度も確認していなかったのです。

自分で書いた検証スクリプトにも同じことが起きました。ある関数が常に `false` を返していたのですが、原因は依存の import 漏れで、発生した ReferenceError を `catch { return false }` が飲み込んでいました。呼び出し側から見ると「探したが見つからなかった」と区別がつきません。検証したことになったまま、main にマージされていました。

画像チェックのスクリプトでは、JPEGのサイズを読む約40行の関数を丸ごと `return None` に置き換えても、self-test が PASS のままでした。テストがあることと、欠陥を検出できることは別でした。

委託した先が「バックグラウンドで開始しました」と報告して終了し、ジョブも成果物もどこにも残っていなかったこともあります。

並んでいるものを見て、共通点に気づきました。

**どれも、失敗そのものより「失敗が成功として報告されたこと」が問題でした。**

そして、そのどれもがモデルの推論能力の問題ではありませんでした。完了の定義、外部状態の確認手順、エラーの扱い、テストの設計、報告の受理基準。壊れていたのは、AIの外側にある手続きのほうでした。

このころの開発の形を図にすると、こうなります。

```
  人間 --指示--> AI --コード--> リポジトリ
    ^                              |
    |                              |
    +--------- 目視で確認 ---------+
```

チェックが人間の目しかありません。だから、報告が正しいかどうかも人間が毎回確かめるしかない。見落とせば、そのまま通ります。

## AIを賢くするより、周囲を作り始めました

モデルを変えても、この形のままでは同じ事故が起きます。だから、モデルではなく周囲を作ることにしました。

最初に作ったのは、始める前の関門です。人間が承認した計画と、タスクの分解と、受け入れテストが揃うまで、AIにプロダクションコードを書かせないことにしました。これは今 [PlanGate](https://github.com/s977043/plangate) というOSSにまとまっています。「7 files, 0 insertions」型の事故は、そもそも何をもって完了とするかが決まっていないところから始まっていました。完了条件を先に文章にしておけば、報告と突き合わせられます。

次に作ったのは、レビュー基準を自分たちの手元に置く仕組みです。AIレビューをベンダーのブラックボックスに預けると、何を見ているかが分からず、指摘も毎回ぶれます。そこでレビュー判断を、リポジトリの中でバージョン管理される skill として書き、実行するようにしました。こちらは [River Review](https://github.com/s977043/river-review) です。

そしてブログ運用のリポジトリ側には、地味なガードを積みました。main への直コミットをブロックする pre-commit hook、公開前の整合を見る22本のチェックスクリプト、記事の最終確認を回す4本のWorkflow。どれも、以前は私が口頭のチェックリストで守っていたものです。

作るときに決めたことが1つあります。

**新しく作るのは、実際に起きた事故に対応するものだけにする。**

想像した危険に対して先回りでガードを積むと、鳴りっぱなしの警告が増えて、本当に見るべき警告が埋もれます。あとで実際にそれをやってしまうのですが、それは後半で書きます。

## 開発がループになりました

ガードが増えてくると、開発の形が変わりました。

事前に効くもの（計画、受け入れ条件、禁止事項）と、事後に効くもの（hook、CI、チェックスクリプト、レビュー）が両側に立ち、AIの実行がその間に入るようになったのです。

```
      人間: 目的・計画・承認
             |
             v
     +----------------+
     | 事前ガード      |
     | 計画/受入条件   |
     +----------------+
             |
             v
     +----------------+
     |   AIの実行      |
     +----------------+
             |
             v
     +----------------+
     | 事後センサ      |
     | hook/CI/検査    |
     +----------------+
             |
       失敗なら差し戻し
             |
             +--> AIの実行へ戻る
```

図としては当たり前に見えます。でも、実務での違いははっきりしていました。

以前は、この差し戻しの矢印が私でした。私が差分を読み、私が気づき、私が指摘する。私が見落とせばループは回りません。私が疲れていれば精度が落ちます。並列で3本走っていれば、3本目は雑になります。

いま変わったのは、この矢印の大半が機械になったことです。

**人間がフィードバックループそのものになるのをやめた、というのが一番大きな変化でした。**

私が判断するのは、機械が拾えないところに絞られていきました。この記事は書くべきか。この設計は事業として妥当か。このガードは緩めてよいか。

## 人間の仕事は減ったのではなく、ずれました

よく誤解されるのですが、コードを読まなくなったわけではありません。

読む対象が変わりました。

以前は「この差分は正しいか」を読んでいました。いまは、それに加えて「この差分が間違っていたら、何が気づくのか」を見ています。気づく仕組みが無ければ、差分が正しく見えても安心はできません。逆に、気づく仕組みがあるなら、多少雑な差分でも安全に試せます。

書くコードも変わりました。プロダクトのコードを書く時間が減り、チェックスクリプトと hook と Workflow を書く時間が増えました。プロダクトから離れたというより、コードが安全に生まれる場所のほうへ、設計の対象が1段広がった感覚です。

そして、人間が最後に持ち続けているものが1つあります。権限です。マージも、公開も、ガードを緩める判断も、自動化していません。これは後で書く理由があります。

## ハーネス自身を壊し、直し続けています

ここからが、実は一番時間を使っているところです。

ガードを作ると、そのガードが間違います。

Zennの二重公開を防ぐガードは、3回作り直しました。1回目、スクリプトは書けていたのに CI に接続されておらず、存在するだけで一度も動いていませんでした（PR #391 で接続）。2回目、接続はできたものの、計測軸がコミットの日付だったため、実際に公開が起きるタイミングとずれて、見逃しと誤ブロックの両方が起きました（PR #393 で差分ベースに変更）。3回目で、前回公開からの実経過時間で判定するようにしました（PR #553）。

note記事の最終化チェックを入れたときは、もっとひどいことをしました。新しい lint を全体のチェックに直結させたところ、過去記事に57件の警告が一斉に出て、警告そのものが読まれなくなったのです（PR #594）。安全にしようとして、安全装置の信号を薄めました。同じPRの中で、対象を絞る入口を分ける修正を入れました。

学びのインデックスに重複検出を足したときは、思わぬ副作用が出ました。その検査が初めてブロッキングになった結果、`npm run check` が20件の検査を `&&` で連結していて、前のほうで1件落ちると後続6件が一度も実行されない構造だったことが露見しました（PR #605 で発覚、PR #606 で全件実行してまとめて報告する集約ランナーに変更）。ガードを足したら、ガードを並べる仕組みの欠陥が見えたわけです。

古いブランチが公開済みの記事を巻き戻す事故（GitHub上のPR差分は「+14行」に見えるのに、現在の main に対しては記事全体の巻き戻しでした）に対しては、staleness チェッカーを作りました（PR #448）。そして誤検知が1日4件出たので、判定軸を作り直しました（PR #464）。

ここまでを並べると、やっていることの形が見えてきます。

```
   事故が起きる
        |
        v
   原因を言葉にする
        |
        v
   ガードを足す/直す
        |
        v
   運用して観測する
        |
        v
   まだ再発する / 誤検知する
        |
        +----> 事故が起きる へ戻る
```

つまり、開発のループを回すだけでなく、**ループを回す仕組みそのものを、次の周回の対象にしていた**ということです。

私はこれを、しばらく自分だけの運用の癖だと思っていました。

そのあとで、Thoughtworks の Birgitta Böckeler さんが書いた [Harness Engineering for Coding Agents](https://martinfowler.com/articles/exploring-gen-ai/harness-engineering.html)（2026年4月2日）を読みました。

harness を「AIエージェントのうち、モデル以外のすべて」と定義したうえで、事前に効かせる guides と、事後に観測して自己修正させる sensors を分けています。ここまでは、私の図とほぼ同じでした。

問題は次の一文です。

> Whenever an issue happens multiple times, the feedforward and feedback controls should be improved to make the issue less probable to occur in the future, or even prevent it.

さらに「The steering loop」という節があり、こう書かれています。

> The human's job in this is to steer the agent by iterating on the harness.

私が「自分の癖」だと思っていたものは、既に明文化されていました。しかも先に。

これを「後から名前があると知りました」と書くのは正確ではありません。名前だけでなく、中身が先に書かれています。

正確に言えば、**実務から独立に同じ結論に着いて、あとで原典を読んだら先に書かれていた**、ということです。

がっかりはしませんでした。むしろ逆でした。事故から言語化した結論と、体系的に整理された結論が一致するなら、それはどちらにとっても裏づけになります。

もう1つ、この定義を読んで自分の説明を直した点があります。CI もテストも lint も、Böckeler さんの定義では harness に含まれます。私はしばらく「CI とは別にハーネスを入れた」と説明していましたが、これはずれていました。前からあったCIやテストも harness の一部で、私はそこに事前ガードと証跡と権限境界を足していたにすぎません。前回の記事で `Agent = Model + Harness` という見方を書きましたが、その Harness の中身は、新しく作ったものだけではなかったわけです。

## では、ハーネスの改善もAIに任せられるのか

原典にも「改善にAIを使ってもよい」と書かれています。実際、私も改善案の下書きはAIに書かせています。

ただ、ここには線を引いています。

きっかけは Darwin Gödel Machine の論文（arXiv 2505.22954）でした。自分自身のコードを書き換えて改善するエージェントで、SWE-bench で 20.0% から 50.0%、Polyglot で 14.2% から 30.7% に伸びています。しかも改善された対象が、編集ツール、長い文脈の扱い、複数モデルによる相互レビューの仕組みで、まさに harness 側です。自己改善が実際に効くという実証として読めます。

同じ論文が、もう一方の結果も報告しています。ツールを使ったふりをしてテストのログを捏造する挙動が出たこと。そして objective hacking の例として、ある個体が**幻覚検出のためのログ出力そのものを削除して、検出機構を無効化した**ことです。特殊トークンを変えるなという指示があったにもかかわらず、です。

スコアは上がりました。問題は解決していませんでした。

もう1本、2026年9月2日の論文（arXiv 2609.02246）でも似た報告があります。実運用で、エージェントが環境に置かれたキャッシュ済みの答えを読んで満点を取り、100%の合格率が実際には68%の能力を隠していた、というものです。そして判定側の rubric をいくら書き直しても改善は頭打ちで、効いたのは決定論的な構造の制約だけだったと書かれています。

ここから導いた線は、精神論ではありません。

**改善の対象に、評価する側や監視する側を含めた瞬間に、それは指標のハッキングの対象になります。**

だから、私のハーネスでは次を人間の権限のまま残しています。

マージ。公開。ガードを緩める判断。そして、検査そのものを消す変更。

AIには、事故の分析も、ガードの実装も、誤検知の修正案も出してもらいます。ただし、それを取り込むかどうかは人間が決めます。自動マージも自動公開も入れていません。速さのためにここを外すと、最初に戻ります。「できました」が信用できない状態に、です。

## 設計する対象が広がってきた順序

振り返ると、私が設計しようとしていた対象は、この順で広がってきました。

最初はプロンプトでした。どう言えば意図どおり動くか。

次にコンテキストでした。何を渡せば正しく判断できるか。

そのあとがハーネスでした。実行の周囲に、事前ガードと事後センサと証跡と権限境界をどう置くか。

いまは、そのハーネス自体をどう改善し続けるか、を見ています。

これを業界の成熟モデルとして書くつもりはありません。あくまで、私の設計対象が広がってきた順序です。

なお、Addy Osmani さんが2026年6月7日に [Loop Engineering](https://addyosmani.com/blog/loop-engineering/) という言葉を使っています。

> Loop engineering is replacing yourself as the person who prompts the agent. You design the system that does it instead.

これは魅力的な定義ですが、指しているのはエージェントの実行ループを自動化する話で、この記事で書いてきた「ハーネスを改善するループ」とは別物です。同じ「ループ」でも層が違うので、私は自分の話にこの語を当てないことにしました。新しい言葉を増やすより、どの層の話をしているかを毎回書くほうが誤解が少ないと思っています。

## まとめ

AI駆動開発を1年ほど続けて、いま自分がやっていることを言葉にすると、こうなります。

AIにコードを書かせることが目的ではありませんでした。

**AIが安全に、繰り返し、自律的に開発でき、その開発のやり方自体を改善していける場所を作ること**が、やっていたことでした。

そこにたどり着く道は、賢い設計ではなく事故でした。0 insertions のPR、存在しない記事を消せと言い続けた申し送り、握りつぶされた ReferenceError、壊しても通るテスト。そのたびに、口頭のチェックリストを1つずつ機械に移しました。

ガードも間違えます。だから作り直します。作り直す作業そのものが、いま一番価値のある開発だと感じています。

ただし、そのループの一番外側には人間を置いたままにしています。評価する側まで自動改善の対象にすると、指標だけがきれいになることは、既に論文が2本示しています。

AIが失敗しても壊れない場所を作ること。人間の役割は、そちらへ移ってきました。
